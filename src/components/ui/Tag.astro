---
interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<a
  href={`/tag/${tag}`}
  class="inline-block px-3 py-1 text-xs font-heading font-medium rounded-full bg-gray-200 text-gray-600 hover:bg-gray-300 no-underline transition-colors"
>
  {tag}
</a>
